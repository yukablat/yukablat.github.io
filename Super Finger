<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Ashton K. I — Portfolio</title>
  <meta name="description" content="Portfolio of Ashton K. I — projects, skills, and experience." />

  <!-- ====== Design Tokens ====== -->
  <style>
    :root {
      --primary: #5b8def;          /* TODO: change to your preferred accent color */
      --bg: #0f1221;               /* dark background */
      --surface: #161a2f;          /* cards/sections */
      --text: #e8ebff;             /* primary text */
      --muted: #b4b9d9;            /* secondary text */
      --ring: rgba(91, 141, 239, .4);

      --radius: 14px;
      --shadow: 0 10px 30px rgba(0,0,0,.25);
      --maxw: 1080px;
      --font: ui-sans-serif, system-ui, Segoe UI, Roboto, Helvetica, Arial, Apple Color Emoji, Segoe UI Emoji;
      --mono: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", monospace;
    }

    /* Light theme */
    .theme-light {
      --bg: #f7f8fc;
      --surface: #ffffff;
      --text: #14172b;
      --muted: #4b4f68;
      --ring: rgba(91, 141, 239, .25);
      background: var(--bg);
      color: var(--text);
    }

    /* Base */
    html, body {
      height: 100%;
      background: var(--bg);
      color: var(--text);
      font-family: var(--font);
      line-height: 1.6;
    }
    * { box-sizing: border-box; }

    a { color: var(--primary); text-decoration: none; }
    a:hover { text-decoration: underline; }
    img { max-width: 100%; display: block; }

    .container {
      width: 100%;
      max-width: var(--maxw);
      margin: 0 auto;
      padding: 24px;
    }

    header {
      display: grid;
      grid-template-columns: 120px 1fr auto;
      gap: 24px;
      align-items: center;
      background: linear-gradient(180deg, rgba(91,141,239,.12), transparent 40%);
      padding: 32px 0 12px;
    }

    .avatar {
      width: 120px; height: 120px; border-radius: 24px; overflow: hidden;
      border: 1px solid rgba(255,255,255,.08);
      box-shadow: var(--shadow);
      background: #22263f;
    }
    .avatar img { width: 100%; height: 100%; object-fit: cover; }

    .title { margin: 0; font-size: clamp(1.6rem, 2.5vw, 2.2rem); }
    .subtitle { margin: 6px 0 0; color: var(--muted); }

    .actions { display: flex; gap: 12px; align-items: center; }
    .btn {
      display: inline-flex; align-items: center; gap: 8px;
      background: var(--primary); color: white; padding: 10px 14px;
      border-radius: 999px; border: none; cursor: pointer;
      box-shadow: 0 6px 18px var(--ring);
    }
    .btn-ghost {
      background: transparent; color: var(--text);
      border: 1px solid rgba(255,255,255,.12); padding: 10px 14px;
      border-radius: 999px; cursor: pointer;
    }

    /* Section */
    section { margin: 28px 0; }
    .section-title {
      font-size: 1.2rem; font-weight: 700; margin: 0 0 12px;
      letter-spacing: .3px;
    }
    .card {
      background: var(--surface);
      border: 1px solid rgba(255,255,255,.08);
      border-radius: var(--radius);
      padding: 20px;
      box-shadow: var(--shadow);
    }

    /* About */
    .about-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 16px;
    }
    .kpis { display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px; }
    .kpi { background: rgba(255,255,255,.04); border: 1px solid rgba(255,255,255,.08); border-radius: 10px; padding: 12px; text-align: center; }
    .kpi .num { font-weight: 800; font-size: 1.3rem; }

    /* Projects */
    .projects {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 16px;
    }
    .project {
      display: grid; grid-template-rows: 160px auto;
      overflow: hidden;
    }
    .project .thumb {
      background: #22263f; border-radius: 10px; overflow: hidden;
      border: 1px solid rgba(255,255,255,.07);
    }
    .project .meta { padding-top: 12px; }
    .tags { display: flex; flex-wrap: wrap; gap: 6px; margin-top: 8px; }
    .tag {
      font: 12px var(--mono); background: rgba(91,141,239,.14);
      color: #cfe0ff; border: 1px dashed rgba(91,141,239,.35);
      padding: 2px 8px; border-radius: 999px;
    }

    /* Experience */
    .timeline { display: grid; gap: 12px; }
    .item { display: grid; gap: 6px; padding: 16px; border-radius: 12px; background: rgba(255,255,255,.04); border: 1px solid rgba(255,255,255,.08); }
    .item .where { font-weight: 700; }
    .item .when { color: var(--muted); font-size: .95rem; }

    /* Skills */
    .skills { display: grid; grid-template-columns: repeat(4, 1fr); gap: 12px; }
    .skillcat { background: rgba(255,255,255,.04); border: 1px solid rgba(255,255,255,.08); border-radius: 12px; padding: 12px; }
    .skillcat h4 { margin: 0 0 8px; }
    .skillcat ul { margin: 0; padding-left: 18px; }

    /* Education & Contact */
    .edu, .contact { display: grid; gap: 8px; }
    .socials { display: flex; gap: 10px; flex-wrap: wrap; }
    .chip { border: 1px solid rgba(255,255,255,.14); padding: 8px 12px; border-radius: 999px; }

    /* Footer */
    footer { color: var(--muted); font-size: .95rem; padding: 24px 0 40px; }

    /* Responsive */
    @media (max-width: 1000px) {
      .projects { grid-template-columns: repeat(2, 1fr); }
      .skills { grid-template-columns: repeat(2, 1fr); }
      header { grid-template-columns: 80px 1fr auto; }
      .avatar { width: 80px; height: 80px; border-radius: 16px; }
    }
    @media (max-width: 640px) {
      header { grid-template-columns: 1fr; gap: 12px; }
      .actions { flex-wrap: wrap; }
      .about-grid { grid-template-columns: 1fr; }
      .projects { grid-template-columns: 1fr; }
      .kpis { grid-template-columns: repeat(3, 1fr); }
    }

    /* Focus styles for a11y */
    :focus-visible {
      outline: 2px solid var(--primary);
      outline-offset: 2px;
    }
  </style>
</head>
<body>
  <div class="container" id="app">
    <!-- ===== Header ===== -->
    <header aria-label="Header">
      <div class="avatar" aria-hidden="true">
        <!-- TODO: Replace with your headshot URL or remove the <img> to keep the placeholder -->
        <img src="" alt="Headshot of Ashton K. I" />
      </div>
      <div>
        <h1 class="title">Ashton K. I</h1> <!-- TODO: Your name -->
        <p class="subtitle">Grade 12 Student • Aspiring [Your Field]</p> <!-- TODO: Your role/tagline -->
      </div>
      <div class="actions">
        <button class="btn" id="themeToggle" aria-label="Toggle theme">🌗 Theme</button>
        <a class="btn-ghost" href="#contact">Contact</a>
        <a class="btn-ghost" href="#projects">Projects</a>
      </div>
    </header>

    <!-- ===== About ===== -->
    <section id="about" aria-labelledby="about-title">
      <h2 class="section-title" id="about-title">About</h2>
      <div class="card about-grid">
        <div>
          <p>
            <!-- TODO: 2–4 sentence bio -->
            I’m a Grade 12 student passionate about [AI/Robotics/Web/Design/etc.]. I enjoy building things that are useful and beautiful.
            Recently, I’ve worked on [brief highlight]. I’m currently learning [technologies/subjects].
          </p>
        </div>
        <div>
          <!-- Optional KPI-style highlights -->
          <div class="kpis">
            <div class="kpi">
              <div class="num">6+</div>
              <div class="lab">Projects</div>
            </div>
            <div class="kpi">
              <div class="num">3</div>
              <div class="lab">Awards</div>
            </div>
            <div class="kpi">
              <div class="num">2</div>
              <div class="lab">Hackathons</div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- ===== Projects ===== -->
    <section id="projects" aria-labelledby="projects-title">
      <h2 class="section-title" id="projects-title">Projects</h2>
      <div class="projects">
        <!-- PROJECT CARD -->
        <article class="card project">
          <div class="thumb">
            <!-- TODO: Thumbnail image -->
            <!-- <img src="project1.jpg" alt="Screenshot of Project 1"> -->
          </div>
          <div class="meta">
            <h3>Project Title</h3> <!-- TODO -->
            <p>A short, outcomes-focused description (what it does + impact).</p> <!-- TODO -->
            <div class="tags">
              <span class="tag">HTML</span>
              <span class="tag">CSS</span>
              <span class="tag">JS</span>
            </div>
            <p>
              <a href="#" target="_blank" rel="noopener">Live</a> ·
              <a href="#" target="_blank" rel="noopener">GitHub</a>
            </p>
          </div>
        </article>

        <!-- Duplicate/modify the card below for more projects -->
        <article class="card project">
          <div class="thumb"></div>
          <div class="meta">
            <h3>Project Title</h3>
            <p>Describe what problem it solves and what you learned.</p>
            <div class="tags">
              <span class="tag">Python</span>
              <span class="tag">Flask</span>
            </div>
            <p>
              <a href="#" target="_blank" rel="noopener">Demo</a> ·
              <a href="#" target="_blank" rel="noopener">Repo</a>
            </p>
          </div>
        </article>

        <article class="card project">
          <div class="thumb"></div>
          <div class="meta">
            <h3>Project Title</h3>
            <p>Key features in one line. Keep it concise.</p>
            <div class="tags">
              <span class="tag">C++</span>
              <span class="tag">Arduino</span>
            </div>
            <p>
              <a href="#" target="_blank" rel="noopener">Video</a> ·
              <a href="#" target="_blank" rel="noopener">Code</a>
            </p>
          </div>
        </article>
      </div>
    </section>

    <!-- ===== Experience ===== -->
    <section id="experience" aria-labelledby="exp-title">
      <h2 class="section-title" id="exp-title">Experience</h2>
      <div class="timeline">
        <div class="item">
          <div class="where">Role — Organization</div> <!-- TODO -->
          <div class="when">Month Year – Month Year</div> <!-- TODO -->
          <ul>
            <li>Led/created <b>X</b>, achieving <b>Y measurable outcome</b>.</li>
            <li>Collaborated with <b>team/stakeholders</b> to deliver <b>result</b>.</li>
            <li>Tools: <i>Tool A</i>, <i>Tool B</i></li>
          </ul>
        </div>
      </div>
    </section>

    <!-- ===== Skills ===== -->
    <section id="skills" aria-labelledby="skills-title">
      <h2 class="section-title" id="skills-title">Skills</h2>
      <div class="skills">
        <div class="skillcat">
          <h4>Programming</h4>
          <ul>
            <li>Python (Intermediate)</li>
            <li>JavaScript (Intermediate)</li>
            <li>Java (Beginner)</li>
          </ul>
        </div>
        <div class="skillcat">
          <h4>Web</h4>
          <ul>
            <li>HTML, CSS (Responsive)</li>
            <li>React (Beginner)</li>
          </ul>
        </div>
        <div class="skillcat">
          <h4>Tools</h4>
          <ul>
            <li>Git, GitHub</li>
            <li>Figma</li>
          </ul>
        </div>
        <div class="skillcat">
          <h4>Soft Skills</h4>
          <ul>
            <li>Leadership</li>
            <li>Communication</li>
          </ul>
        </div>
      </div>
    </section>

    <!-- ===== Education & Contact ===== -->
    <section id="education" aria-labelledby="edu-title">
      <h2 class="section-title" id="edu-title">Education</h2>
      <div class="card edu">
        <div><b>Your High School</b> — Kingston, ON</div>
        <div>Expected Graduation: 2026</div>
        <div>Relevant Courses: ICS4U, MHF4U, MCV4U, SBI4U, SCH4U</div>
        <div>Awards: Honour Roll, [Scholarship/Award]</div>
      </div>
    </section>

    <section id="contact" aria-labelledby="contact-title">
      <h2 class="section-title" id="contact-title">Contact</h2>
      <div class="card contact">
        <div class="socials">
          <!-- TODO: Replace with your links -->
          <a class="chip" href="mailto:you@example.com">Email</a>
          <a class="chip" href="#" target="_blank" rel="noopener">GitHub</a>
          <a class="chip" href="#" target="_blank" rel="noopener">LinkedIn</a>
          <a class="chip" href="#" target="_blank" rel="noopener">Devpost</a>
        </div>
      </div>
    </section>

    <footer>
      © <span id="year"></span> Ashton K. I. All rights reserved.
    </footer>
  </div>

  <!-- ===== Small JS: theme toggle + year ===== -->
  <script>
    // Persisted theme
    const themeToggle = document.getElementById('themeToggle');
    const root = document.documentElement;
    const saved = localStorage.getItem('theme') || 'dark';
    if (saved === 'light') document.body.classList.add('theme-light');

    themeToggle?.addEventListener('click', () => {
      document.body.classList.toggle('theme-light');
      const isLight = document.body.classList.contains('theme-light');
      localStorage.setItem('theme', isLight ? 'light' : 'dark');
    });

    // Year
    document.getElementById('year').textContent = new Date().getFullYear();
  </script>
</body>
</html>
